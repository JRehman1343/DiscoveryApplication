server: 
  port: 8761

spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8881
      
management:
  endpoints:
    web:
      exposure:
        include: "*"
      
eureka:
  client:
    register-with-eureka: false